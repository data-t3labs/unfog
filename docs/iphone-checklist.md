# Unfog — iPhone acceptance checklist

Real-device pass on the friend's phone (iOS 26.x, Safari). Each item: do the step, compare with the expected
result, note the iOS build if something differs. Caveats at the end come from the iOS PWA research
(task artifact `research/ios-pwa-research.md`).

Before starting: `https://data-t3labs.github.io/unfog/` deployed from `main`; a `Sync.zip` (Files → iCloud Drive →
Fog of World → long-press **Sync** → Compress) on the phone; a charged phone with Low Power Mode **off**.

| # | Step | Expected | Result / iOS build |
|---|------|----------|--------------------|
| 1 | Open the URL in Safari. | Map loads (OpenFreeMap bright), fog over the whole map (nothing visited yet), "Where to?" pill, Fog/Heat/Off, locate button, stat chip "0.00 km² explored / 0 cells" with the hint "Import your Fog of World history, or turn on tracking in Settings" above it (tap → Data), tab bar. No Record button anywhere. Install card appears at the bottom (Safari, not installed); dismissing it shows the first-run "Track my movement?" card (Turn on / Not now — once). Stats shows "Nothing explored yet" with a Go to Data button. | |
| 2 | Safari: page menu → Website Settings → Location → **Allow** (optional, avoids re-prompts in the tab). | Setting saved. | |
| 3 | Share → **Add to Home Screen** → keep "Open as Web App" on → Add. | Icon "Unfog" (navy, fog + orange route) on the Home Screen. | |
| 4 | Launch from the Home Screen. | Full screen, no Safari chrome, status bar over the map, controls clear of the notch and home indicator. Install card does NOT appear. | |
| 5 | Tap the locate button. | iOS location prompt → Allow. Blue dot with halo at your position, map eases to it, locate icon turns blue (follow). Pan the map → follow turns off. | |
| 6 | Data → Import files → Choose File → pick `Sync.zip`. | Progress text, then "N new cells, X km² added" toast (above the tab bar, never over Import files) and per-file lines ("Sync.zip: Fog of World — N tiles"). Map: fog lifted along your history with soft edges; stat chip "X km² explored / N cells"; the empty-state hint is gone. Re-import the same zip → "0 new cells" (no double counting). | |
| 7 | Zoom in/out around your area (z12 → z18). | Overlay stays seamless across tile edges, labels stay above the fog, no blocky cells; pinch/zoom stays smooth (no WebGL context loss). | |
| 8 | Fog → **Heat**. | Amber→red glow by visit count over a dimmed map; legend "visits 1 · 2–3 · 4–6 · 7+". **Off** removes the overlay. | |
| 9 | Help → Settings: Dark basemap, fog softness, reveal, fog strength, tight core, miles. | Each change re-renders the overlay; chrome switches light/dark with the basemap; distances switch units. Settings survive a relaunch. | |
| 10 | "Where to?" → type a nearby place (≥3 letters). | Photon results with name + neighbourhood, biased to the map area. Tap one. | |
| 11 | Route sheet. | Title + "x km direct", one line "Routes follow paths where they exist and straight lines where they don't, timed at walking pace" (no Walk/Bike/Drive anywhere), detour slider "+25% · up to y km", 2–3 candidates (Most new / Balanced / Direct) with "km · min" (walking pace) and "% new / km unexplored"; selected route orange with glow, alternatives amber/blue; pin at the destination; camera fits the routes above the sheet. Move the slider → routes recompute. A pin off the street grid: the status line says "starts/ends with N m off-path" and that leg is dashed. | |
| 11b | "Where to?" → **Explore a loop from here** (also offered in the route sheet when no route is found). | Sheet "Explore from here · loops of about 3 km": the line "Round trips on streets and paths, timed at walking pace", length chips 2 / 3 / 5 / 8 km + a 1–15 km slider, then 1–3 rows "Loop A / B / C" with "km · min" and "% new / km unexplored"; round trips starting at the blue dot (a pin marks the start only when there is no location and the map centre is used), drawn with the same orange/amber/blue styling and fitted above the sheet; pill reads "Loop from here". Tap **5 km** → loops recompute near 5 km; drag the slider → the label follows and loops recompute when you let go; the length is remembered next time. **Go** → follow bar "Loop A · x km · y min", "z% new · round trip from here"; **End** restores the chrome. | |
| 11c | Route sheet (Google Maps app installed) → **Google Maps** (the row under Go). Then plan a long route (e.g. across the East River, > 8 km) and look under Go again. | The Google Maps app opens (not Safari) with walking directions along the route's corners, turn-by-turn ready to start; the route began at your position, so no origin is sent and Google uses the phone. A long route shows "1 of N · x km" (arrow icon), "2 of N · y km"… buttons in that row and the line "Google Maps: N parts (9 checkpoints per trip)."; each later part opens as a preview from the previous part's end. The row stays one line high, so the loops/routes are still fitted above the sheet. **Apple Maps** opens walking directions to the destination only. **Save GPX** → share sheet → Save to Files gives `unfog-route-<place>.gpx`. Loops offer Google Maps + Save GPX, no Apple Maps. Without the Google Maps app the link opens Google Maps in Safari, which honours only 3 checkpoints: note it. | |
| 12 | Plan a route in a city with no prebuilt region and nothing downloaded (e.g. long-press a pin in Philadelphia or Boston after panning there; Wi-Fi or mobile, Low Data Mode off). | The route appears by itself after a short wait: no prompt, no download button, no "No routing data" line. Data → Routing data reads "Automatic: the streets around you download as you go…" and lists "Streets near New York (US)"-style rows with a size and "used today", then "N MB of streets on this phone" with **Clear**, then "Coverage list updated …". The prebuilt Regions list is still underneath. Outside North America (e.g. a pin in Paris) the old offer stays: "No routing data for this area yet" with **Download this area** and **Route anyway (straight line)**. | |
| 12b | Data → Routing data → **Clear**. Then plan the same route again. | Toast "Routing data cleared — it downloads again as you go"; the list reads "Nothing downloaded yet…" and Clear is gone; the prebuilt Regions below are untouched. The route comes back on its own and the list refills. | |
| 12c | Airplane mode. Plan a route where the streets were just cached (12), then one somewhere in North America you never opened online. | Cached: the real route, offline. Never opened: a dashed straight line "as the crow flies" at once, with a status line saying so — no error, no spinner, no download offer; the map strip and Go still work. Back online, plan it again → the real route replaces the line. | |
| 12d | Walk (or pan the map) a few km into an area you never opened, with tracking off, then check Data → Routing data. | New rows / a bigger total within a minute or so of arriving (the streets around you download in the background); with Low Data Mode on (Settings › Cellular › Cellular Data Options) nothing new arrives until it is off. | |
| 13 | Tap **Go**. | Sheet collapses to a bar (route name · km · min, **End**); the map follows the blue dot; selected route stays. Walk 50 m → dot moves, map follows. **End** restores the chrome. | |
| 14 | Long-press the map. | Destination pin dropped, route sheet opens ("Dropped pin"). Clear with × next to the search pill. | |
| 15 | Help → Settings → **Track my movement** on (first time: iOS location prompt → Allow). Back on the map, walk ~5 minutes with the screen on. | The switch turns green and stays on across relaunches. A small "Tracking" pill (red dot) sits top-left under the search pill; no banner, no Stop, nothing else changes — search, routes and long-press all still work. Screen does not auto-lock (wake lock). Walking a street you have never walked lifts fog behind you within ~5 s (the next checkpoint) and the stat chip's cell count grows. Tapping the pill opens Help → Tracking. | |
| 16 | While tracking: press the side button (lock), wait 30 s, unlock and return. | Pill still "Tracking"; fixes resume; the gap is not bridged with a straight line if > 500 m. Wake lock re-acquired (screen stays on again). | |
| 17 | While tracking: switch to another app for 2 minutes, come back. | Either the same session continues (pill still there), or the app relaunches: no dialog at all — the earlier session is saved (Data → Sessions lists it, "Tracked <date> <time>") and a new session is running (pill back within seconds, no tap needed). | |
| 18 | Help → Settings → **Track my movement** off. | Switch grey, pill gone, toast "Tracking off"; no summary sheet. Data → Sessions lists the session with "Export GPX" → share sheet → Save to Files, and delete. Stats → "tracked sessions" counts it. Switch it back on → next launch the pill is there without any tap. | |
| 19 | Data → **Export backup**. | Share sheet with `unfog-backup-YYYYMMDD.zip` → Save to Files / iCloud Drive. "Last backup today" under the button. Stats → Last backup date updated. | |
| 20 | Delete the Home Screen icon, reinstall from Safari, open, Data → Import → pick the backup zip. | Everything restored (same area in the stat chip; sessions listed). | |
| 21 | Airplane mode, relaunch from the Home Screen, pan around your area. | App loads offline; previously viewed basemap tiles show; unvisited basemap areas are blank but the fog/heat overlay still renders; search shows "needs a connection". | |
| 22 | Data → Routing data → in the prebuilt Regions list under the automatic section, Download New York City (on Wi-Fi), then airplane mode → plan a route in NYC. | Progress to 100 %; "Offline since …" line; routing works offline anywhere in the region, not just where you have been. | |
| 23 | Wait for a new deploy, relaunch. | "Update available — Reload" toast (it never reloads on its own). Reload while tracking is fine: the session so far is saved at the next boot and a new one starts, no dialog. Reload loads the new version without losing data. Help → the last line reads "Unfog 0.1.0 · build <new sha>". | |
| 24 | Import GPX (Apple Health export `workout-routes/*.gpx`) and a Google Timeline JSON. | Each shows as "1 track" / "N tracks"; stats list the sources. | |

## Things the headless suite cannot prove — check on the phone

The real-engine e2e suite (`tests/e2e/real.spec.ts`, Chromium with an iPhone 15 profile) drives the same
code paths, but a few behaviours only exist on iOS. Do these explicitly:

| # | Step | Expected | Result / iOS build |
|---|------|----------|--------------------|
| A1 | Data → Import files → in the Files picker, navigate to iCloud Drive → Fog of World → Sync and try to multi-select two **raw tile files** (no extension, e.g. `23e4lltkkoke`). | Files are selectable (the picker honours `application/octet-stream` in the `accept` list). If they are greyed out, that is an iOS `accept` filter problem: note it, then use Sync.zip instead (the zip path is always allowed). The Data screen says "If Sync.zip is greyed out in the Files picker, open the Sync folder and select the files inside it instead" under Import files, and "How to get Sync.zip out of Fog of World" opens Help with that section expanded. | |
| A2 | Same picker: pick `Sync.zip` **and** a `.gpx` in one go. | Both import; the result list has one line per file. | |
| A3 | Data → Export backup → in the share sheet tap **Cancel** (swipe it away). | Toast "Backup cancelled"; "No backup yet" / the old date stays; Stats → Last backup unchanged. (A dismissed share sheet must not count as a backup.) | |
| A4 | Export backup → **Save to Files** → iCloud Drive. Then Data → Sessions → Export GPX on a session → Save to Files. | Files land with the names `unfog-backup-YYYYMMDD.zip` / `unfog-YYYY-MM-DD-HHMM.gpx`; the app toasts "Backup shared" / "GPX shared". In a Safari tab (not installed) the share sheet still offers files; if it does not, the app falls back to a download banner. | |
| A5 | Fresh install, Home Screen app, location never granted: answer the first-run card with **Turn on** (or flip the switch in Settings) before ever tapping the locate button. | One iOS prompt; after Allow the pill says "Tracking · waiting for GPS", then "Tracking" within a few seconds, and the dot appears where you stand. Deny → toast with the Help link, the switch stays off, no pill. | |
| A6 | Track indoors / just after launch (coarse first fixes). | Fixes worse than 50 m are dropped — the pill says "Tracking · GPS ±N m, waiting for better" until accuracy improves; nothing is drawn meanwhile. | |
| A7 | While tracking, lock the phone for 2 minutes. | On unlock the pill is still there; the trail only grows again after the next fix (iOS gives none while locked). | |
| A8 | While tracking, switch to Camera for 5+ minutes (or take several photos), come back. | If iOS killed the app: relaunch shows no dialog; Data → Sessions has the earlier session with every point up to its last fix, and the pill shows a new session running. | |
| A9 | Airplane mode **before the very first launch** of a fresh install (worst case: basemap style never cached). | The app still opens: plain grey ground instead of streets, fog/heat overlay renders over it, stats + Data work, routing works where graph data is cached. Turning networking back on swaps in the real basemap without a relaunch. | |
| A10 | Airplane mode → "Where to?" → type a place. | Status "Search needs a connection", no crash; long-press still drops a pin and routes if graph data is cached. | |
| A11 | Route somewhere in NYC that was never opened online, while offline, **without** having downloaded the region. | A dashed straight line "as the crow flies" comes back at once (the coverage list knows a pack covers NYC, nothing of it is on the phone): no "No routing data" offer, no spinner stuck. Somewhere outside every pack (e.g. Paris) while offline: "No routing data for this area yet"; tapping **Download this area** fails with a clear "Download failed" line. | |
| A12 | Help → Settings → change a slider with a finger drag (not a tap). | The value label follows the drag; the overlay re-renders only when the finger lifts (one re-render per gesture, no stutter). | |
| A13 | Delete the Home Screen icon → Safari → same URL. | Data is gone (separate container) and the install card is back; importing the backup restores everything. | |
| A14 | Trigger two toasts in a row (e.g. Data → Export backup → Cancel the share sheet, then straight away Export again → Save), and one while a route sheet / the Heat legend is up. | Toasts show one at a time (the second waits for the first), sit just above the tab bar / stat chip / sheet — never over a button — and never touch the locate button or the legend. With the phone in landscape the sheets, screens and search panel keep clear of the notch (safe-area padding). | |
| A15 | Settings › Accessibility › Display & Text Size › Larger Text → drag to the largest non-accessibility size, then open Unfog (map, route sheet, Data, Help → Settings). | Every string grows with the text size (the type scale is rem on `-apple-system-body`); buttons, rows and the search pill grow with it instead of clipping; the tab bar labels stay readable. Set it back afterwards. | |
| A16 | Settings › Accessibility › Motion › Reduce Motion on → open Unfog, route somewhere, turn tracking on and off. | The camera jumps instead of easing (MapLibre honours the setting); the pill's red dot does not pulse; the switch snaps instead of sliding; sheets and toasts appear in place. | |
| A17 | Help → Settings in the **Bright** basemap. | The chosen option in Basemap / Units / Cleared core is a dark pill with light text (not white on white); switching to Dark shows a light pill with dark text. | |
| A18 | "Where to?" → **Drop a pin at the map centre** (pan the map somewhere first). | Route sheet opens for "Dropped pin" at the centre of the map you left; touch-and-hold on the map still drops a pin anywhere. With location denied the sheet says "Routes start at the map centre — no location yet. Tap the locate button to start from where you are." | |
| A19 | Tracking on, leave the app open on the map across local midnight. | Data → Sessions shows the day's session ended at midnight and a new one running from 00:00 — no dialog, pill unchanged. | |
| A20 | Tracking on, then Settings › Privacy › Location Services → off for Unfog; open the app. | Pill "Tracking paused — location is off" (tap → Help › Tracking); switch stays on. Allow again and return → pill back to "Tracking" without a tap. | |

## Known iOS caveats (design expectations, not bugs)

- **No background location.** Tracking only works while the app is open and the screen is on. Locking the phone or switching apps pauses fixes; long gaps are not bridged. Fill gaps with Apple Health / Strava GPX or Google Timeline imports (Help → Always recording).
- **Process death.** After a long time in the background the app relaunches from scratch (WebGL-heavy pages are killed first). The session is persisted on every fix and saved as a track at the next launch, where a new session starts (no dialog); the camera position is restored.
- **Wake lock** fails in Low Power Mode / critical battery (the pill says "keep the screen on") and is released on every hide; it is re-requested when the app becomes visible again. Works in Home Screen apps only since iOS 18.4.
- **Location permission.** No install-time prompt; the first request comes from a tap. `permissions.query` is unreliable on WebKit, so the app always asks and handles "denied" with the Help panel. iOS 26.0.x had an installed-app "denied" regression — Settings › General › Transfer or Reset › Reset › Reset Location & Privacy fixes it. Precise Location off ⇒ accuracy in km ⇒ fixes are dropped (> 50 m).
- **Separate container.** The Home Screen app does not share storage or permissions with the Safari tab; importing/allowing must be done inside the installed app. Deleting the icon deletes the data — hence the backup nag after 14 days.
- **No install prompt / share target.** Install is manual (Share → Add to Home Screen); files cannot be shared *to* Unfog, only picked from Files. No orientation lock (manifest `orientation` is ignored), no splash image unless `apple-touch-startup-image` PNGs are added.
- **Street data uses mobile data.** The automatic routing layer fetches a few MB of streets around you and along each route (North America), on Wi-Fi and cellular alike; it pauses in Low Data Mode and never runs offline. The cache trims itself, so Clear is never required.
- **Cross-origin links** (attribution) open an in-app browser overlay, not Safari.
- **Storage.** Home Screen apps get a large quota and `persist()` is auto-granted; the ITP 7-day wipe applies to the Safari-tab version only — install, don't bookmark.
